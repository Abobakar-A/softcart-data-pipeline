version: 2

models:
  - name: dim_customers
    config:
      materialized: table
      schema: marts
  - name: dim_products
    config:
      materialized: table
      schema: marts
  - name: fct_orders
    config:
      materialized: table
      schema: marts
  - name: fct_clickstream_events
    config:
      materialized: incremental
      schema: marts
  - name: fct_returns_analysis
    config:
      materialized: table
      schema: marts